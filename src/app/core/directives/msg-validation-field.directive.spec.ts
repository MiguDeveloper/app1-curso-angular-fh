import { MsgValidationFieldDirective } from './msg-validation-field.directive';

describe('MsgValidationFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new MsgValidationFieldDirective();
    expect(directive).toBeTruthy();
  });
});
